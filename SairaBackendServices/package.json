{
  "name": "sairabackendservices",
  "version": "1.0.0",
  "description": "This directory houses the backend services for **Saira – The Emotion-Aware Life Companion AI**. It provides Node.js (TypeScript) and Python-based services to handle AI inference, audio I/O, SQLite data operations, and native addon integrations. Designed for cross-process communication with the macOS frontend, this backend acts as the brain and memory core of the assistant.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start:dev": "ts-node-dev --respawn src/node_services/main.ts",
    "tsc": "tsc --noEmit",
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest --config=jest.config.js",
    "test:audio": "NODE_OPTIONS='--loader ts-node/esm' node tests/test_audio_addon.ts",
    "test:playback": "NODE_OPTIONS='--loader ts-node/esm' node tests/test_audio_playback.ts",
    "test:devices": "NODE_OPTIONS='--loader ts-node/esm' node tests/test_audio_devices.ts",
    "lint": "eslint src || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^8.57.1",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.4",
    "eslint": "^9.29.0",
    "globals": "^13.24.0",
    "jest": "^29.7.0",
    "prettier": "2.8.8",
    "ts-jest": "^29.4.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "node-addon-api": "^8.4.0",
    "winston": "^3.17.0"
  }
}
